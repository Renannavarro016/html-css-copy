Ao aplicar 'display: grid', 
nada muda inicialmente porque um grid sem configuração adicional, por padrão, se comporta como uma única coluna e múltiplas linhas.

00:07:49
A propriedade 'grid-template-columns' 
permite definir o número e a largura das colunas, utilizando 'auto' para dimensionamento automático neste exemplo.

00:08:05
Definir 'grid-template-columns: auto auto' 
instrui o CSS a criar um grid de duas colunas, reposicionando os elementos automaticamente de acordo.

00:08:54
Ao especificar 'grid-template-columns: auto auto auto', 
você pode criar um grid de três colunas, onde cada coluna se ajusta automaticamente para ter a mesma largura.

A propriedade `grid-template-rows` 
permite configurar a altura de cada linha, por exemplo, definindo cada linha com 100 pixels de altura.

00:09:47
Por padrão, os itens do grid se esticarão para preencher as dimensões da linha definidas, mas existem maneiras de evitar esse esticamento, se desejado.

00:10:39
A propriedade `grid-gap` 
cria um espaço padrão entre todos os itens do grid, com um exemplo de 20 pixels para cada espaço.

00:11:22
Esta configuração básica de grid demonstra um contêiner com sete itens, três colunas de tamanho automático, linhas com 100 pixels de altura e 20 pixels de espaço entre os elementos.


> Generated by WayinVideo | https://wayin.ai/wayinvideo/